# Copy this file to .env.local for local development.

# Base URL of the Django API (see backend/). Defaults to http://127.0.0.1:8000
# if unset, which matches `python manage.py runserver` locally.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
